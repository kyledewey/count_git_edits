package count_git_edits

/*
import java.io.File

object Counter {
  val DEFAULT_GIT_DIR = "."

  def apply(): Counter = {
    apply(new File(DEFAULT_GIT_DIR))
  }

  def apply(gitDirectory: File): Counter = {
    import org.eclipse.jgit.storage.file.FileRepositoryBuilder
    new FileRepositoryBuilder()
      .setGitDir(gitDirectory)
      .build()
  }
}

class Counter(private val git: Git) {
  def counts(
 */
